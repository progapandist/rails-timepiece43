<%= simple_form_for [@user, @watch] do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :price %>
  <%= f.input :name %>
  <%= f.submit "add a review", class: "btn btn-primary" %>
<% end %>
