<%= simple_form_for [@user, @watch] do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :price, :label => "Price /day" %>
  <%= f.input :photo, as: :attachinary %>
  <%= f.submit "Add this watch!", class: "btn btn-primary" %>
<% end %>
