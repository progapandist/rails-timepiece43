<% watches.each do |watch| %>

<div class="card" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2)), url(<%= cl_image_path( watch.photo, :width => 100, :height => 150, :crop => :fill) %>      );">
  <div class="card-category"><%= watch.category %></div>
  <div class="card-description">
    <h2><%= watch.name %></h2>
    <p><%= watch.price %>€/day</p>
  </div>
  <img class="card-user avatar" src= <%= cl_image_path( watch.user.avatar, :width => 100, :height => 150, :crop => :fill) %> >
  <a class="card-link" href= ></a>
  <%= link_to "", watch_path(watch), class:"card-link"  %>
</div>

<% end %>
