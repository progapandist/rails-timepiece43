<div class="container">
  <div class="row">
    <% @watches.each do |watch| %>
    <div class="col-xs-12 col-sm-4">
      <div class="card" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2)), url( <%= cl_image_path watch.photo.path, crop: :fill %> );">
        <div class="card-description">
          <h2><%= watch.name %></h2>
          <p><%= watch.price %>€/day</p>
        </div>
        <a class="card-link" href= ></a>
        <%#= link_to "", user_watch_path(watch), class:"card-link"  %>
      </div>
    </div>
    <% end %>
  </div>
</div>
